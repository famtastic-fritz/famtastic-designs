# Independent application release evidence

## Local acceptance

- Laravel12.69.2/PHP8.3.32 target, Composer audit no vulnerability advisories; dependency lock retained.
- 32 PHP tests/159 assertions: independent owner auth/reset, CSRF/Origin, denied non-owner, public flag, durable request, authoritative confirmation, real separate-process SQLite race, replay, stale writes, proposal/decline, rollback, outbox failure and nested gateway routing.
- 11 native admin tests: New York timezone/DST, real calendar day boundaries, pending reschedule representation, request contracts and no agency runtime endpoints.
- 22 existing public tests pass; same-origin candidate preserves gallery, analytics and footer/policies.
- Chrome local login -> fixture request -> missing/ambiguous DST rejection -> confirmed appointment -> hard reload -> calendar -> proposed reschedule while original stays reserved -> draft opening create/edit/reload -> logout. 390/768/1280 inspected, no horizontal overflow and no application console errors. Synthetic records local only; no mail sent.
- Independent backend review caught nested PHP gateway base-URL routing; normalization and regression test added before deployment. Runtime sends no form contents to analytics and private admin has none.

## Production status

Dedicated database nineoo_locs and separate runtime/migration users provisioned; private config created with mail/booking disabled. First package preflight correctly rejected macOS AppleDouble metadata before installation. Build excludes copyfile metadata; no installer allowlist was weakened.

Pending. No database or application cutover is claimed by this local record. Old Locs request-only inventory:1 request,0 appointments/events/openings. Must recheck before export. Production source/DB scope, release/backup receipt, migration reconciliation, authentication, scheduler, SMTP acceptance and live same-origin request flow will be appended as observed.
