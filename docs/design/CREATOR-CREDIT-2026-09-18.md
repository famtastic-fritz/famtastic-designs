# Creator credit contract

Owner mandate September 18, 2026. Preserve existing footer/credit text and append
one centered final creator-credit row. No price/tier exemption; cheaper sites are
especially included. Only an explicit owner override naming the artifact can
create an exception, which must be recorded in its release audit.

Original: frontend/public/brand/famtastic-designs-logo-v1.png, 2172×724 RGBA PNG,
SHA256 ebb0477344132d32e449ba19e2b622921585aa71af0decdbcf8abfbe033fa950.
Verified byte-identical to the owner-specified fd-admin-error-brand-proof copy.
No redraw/recolor/crop/filter. This implementation uses 180px width, natural 3:1
ratio, responsive max-width, 44px minimum link target, accessible name “Created by
FAMtastic Designs”, obsidian backing and visible keyboard focus.

Link: https://famtasticdesigns.com/ with optional public attribution. Agency uses
fixed utm_source=famtastic-designs, utm_medium=creator_credit and
utm_campaign=created_by_famtastic. Never derive attribution from route tokens,
request IDs, email, customer names or authenticated state. No scripts/cookies added.

React mounts after Routes, preserving all page footers, including standalone
portal/proof routes. Static HTML uses scripts/creator-credit.mjs at existing build
boundaries. Provider-runner proofs receive a byte-identical local PNG before hashes
and QA; callback and hosted outputs use the canonical hosted asset. The PHP stub
consumer is generated by scripts/sync-creator-credit-php.mjs. Existing BrandedEmail
adds an email-safe final table row without changing content, transport or queue keys.

Floating controls: agency portal and showcase owner pages have fixed mobile bottom
navigation. Only pages containing .portal-mobile-nav or .mobile-owner-nav get credit
clearance: 96px through 900px, 192px through 600px, plus device safe-area inset.
Main public site has no permanent bottom launcher found in source; full-screen
navigation and Solution Finder dialogs remain intentional overlays. No blanket
customer redesign. Final clearance needs CUA visual review after deployment.

Historical public HTML is preserved in source. Build output receives a new hash
receipt rather than retroactively changing an approval. For other existing HTML,
run `node scripts/build-credited-artifact.mjs SOURCE NEW_OUTPUT` and review the new
version. Copied historical manifests are source provenance, not approval of the new
version. Never regenerate old evidence ledgers. Private database proof records are
not automatically rewritten; customer acceptance and financial/auth state stay intact.

Checks: `node --test scripts/test-creator-credit.mjs`,
`npm --prefix frontend run test:creator-credit`, normal Node22 build,
`php scripts/email-preview/test.php`, `node scripts/sync-brand-assets.cjs --check`.
Browser verification is CUA-only following the owner's latest instruction.
