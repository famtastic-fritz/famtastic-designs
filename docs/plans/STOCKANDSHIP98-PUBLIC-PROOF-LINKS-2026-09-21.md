# Public proof links — September 21, 2026

At the owner's explicit request, enabled the existing unlisted, revocable,
view-only public sharing for request 17/customer 15/campaign 56 at
**22:36:36 UTC**. Sharing had been disabled; its version is now 1. The built-in
private-reference and complete-proof checks passed. The service recorded the
actual automation actor (uid 0); selection and proof-review status were unchanged.

Emailed the verified customer and copied the verified owner at **22:41:29 UTC
(6:41 PM EDT)**. Subject: **Your StockandShip98 concepts — direct links, no sign-in**.
The message contains one named direct button each for **A — The Exchange**,
**B — Curated Finds**, and **C — The Drop**, with plain-text fallback links and a
Shay signature. It reuses the deployed BrandedEmail shell and canonical logo.
Gmail message `1a0c621a69be9679` is provider-accepted and verified in the owner's
inbox. Readback confirmed both recipients and all three links in both MIME parts.
The customer's inbox placement, readership and approval remain unverified.

Anonymous requests without authorization or cookie headers returned HTML 200 for
all three exact URLs and 200 for all six referenced image requests, without a
login redirect. All three opened as live pages in the in-app browser. The Drop
also passed a 390px viewport image/overflow check. This is not a physical iPhone
Safari test. The links expose design previews only; customer selection, ordering
and payment authority were not granted.

This exact owner request authorizes these public-sharing URLs as an exception to
the usual account-bound proof email destination. Normal lifecycle templates and
the authenticated portal remain unchanged. No historical outbox row was replayed,
proof rebuilt, customer direction selected, or application code deployed.
The ongoing portal discoverability problem is not fixed by this support action.

The redacted evidence records share activation, anonymous HTML/image checks and
email delivery. Signed bearer URLs and recipient contacts remain outside Git.
The links can be revoked with the existing staff sharing controls; do not disable
or rotate them without a subsequent owner instruction.

Evidence: `../evidence/stockandship98-public-links-2026-09-21/`.
