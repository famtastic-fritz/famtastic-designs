# Request 16: Class of 2000 proof delivery

## Isolated scope

Customer source lives at `FAMtastic-Repos/site-mbsh-class-of-2000`, private origin `https://github.com/famtastic-fritz/site-mbsh-class-of-2000`. Agency branch `codex/class-2000-proof-delivery-20260918` contains only this narrowly scoped operational documentation and two request-specific bootstrap/handoff scripts. It changes no shared worker, mail, payment or pipeline module.

Owner-approved authority: the September 18 client-delivery-first plan. Main coordinating task: `01a0ad44-696a-7ff2-85ca-6e0f2d71e38a`. Main owns final release/send; the lane must not impersonate Fritz approval.

## Exact identity and preservation

Request 16 public UUID `8000fc68-aae3-4f40-a3de-2251bd09076a`; customer/organization 14; prospect 298. The original draft said only “high school reunion website.” `request16-staff-proof-handoff.php` checks the exact verified account and preserves every original intake field; additions are under `staff_assisted_brief`, expressly not customer supplied.

Audit event `website-request:16:staff-assisted-brief:owner-approved-20260918:v1` retains original raw intake and SHA-256, authority and automation actor. No owner UID is forged. The script neither captures payment nor sends mail. The private $199 scope is owner-authorized, not a received payment or activated offer.

## Existing public campaign is not this customer project

Prospect 298 already had public-inquiry campaign 55. A first `createLocalHandoff` attempt correctly refused it; its surrounding transaction rolled back without changing the draft. The account-bound `createForProspect($prospect, $context)` service then created campaign 57 and bound request 16 before any artifact callback. Campaign 55 was not altered, expired or duplicated as an account campaign.

Actual campaign: `pc-public-request-8f25c9b3518e725c`; generation job `local-2ae40c80cfe851f8a2ad248fdc55adf0`; routine queue job 314. Repeated handoff checks must reuse this exact binding; never create another campaign by prospect-only lookup.

## Proof artifacts and verification

The three real directions are Hi-Tide Legacy, Miami After Dark, and Class of 2000: Then & Now. Each includes native no-JavaScript demonstration controls, verified internal navigation, explicit no-payment/no-submission boundaries, and unknown event details labeled honestly. Original managed-image-generation PNGs are preserved; web JPEG derivatives meet the callback asset limits. No Class of 1996 private assets or records were reused.

Customer-repository evidence includes provider preflight/execution, all prompt and asset hashes, Build DNA, callback/package receipt, 12 browser cases and independent pass-1/pass-2 review. Initial independent review blocked weak keyboard focus and an empty provenance manifest; after repair, all nine local gates passed with scores 8.55/8.70/8.80. That review does not establish deployed tenant isolation, email delivery or real RSVP/ticket functionality.

## Narrow operations

Use the explicit CLI runtime `/usr/local/bin/php` at the existing agency root. Upload only exact callback/Build DNA private files, outside the public root. Before any import, confirm the current request/customer/campaign binding and independently reviewed artifact hashes. Use the existing `famtastic:proof-local-import` with exact campaign confirmation and package SHA-256, never a general queue drain. Build DNA registration uses the existing `famtastic:build-dna-register`; registration is not notification acceptance.

Callback import, automated policy release and client email are separate operations. Main's policy route must use authenticated automation identity, not UID 1 or a forged Fritz approval. Repeated callback import must retain the same event ID and return existing variants rather than create another set. Preserve all receipts and requery actual database state before claiming completion.

## Client communication and remaining responsibilities

The personally drafted Shay introduction is in the customer repository and remains **unsent** by this lane. Main owns standard/v2 rendering, plain text, exact-recipient resolution, policy gates, exactly-once notification key and SMTP receipt. Never state full autonomy before proven. Payment after direction choice is the explicit alumni exception; a domain purchase is not required for proofs or staging.

The existing private conversation is thread 20, public UUID `7c998b28-f920-4783-a3d4-58ee3782f3fe`, linked to organization 14. The live schema has no staff-assignee column. Verify configured owner notification routing and durable replies before promising direct Fritz routing; do not invent an assigned-inbox capability or send a fake customer reply.

The commercial lane owns request-bound offer activation without generic checkout renewal leakage. The selected client direction must be preserved into its own staging implementation. Real RSVP/attendee access/tickets require tested backend components, merchant authority, confirmed event details and client acceptance before launch.

## Storage and recoverability

Only this lane's initial newly created unmodified checkout was removed after a disk-full expansion. It was recreated with narrow sparse paths. No original files, existing worktrees or other agents' artifacts were removed. No dependencies were installed; customer source/build is standalone and the optional Playwright QA explicitly uses an existing installation.
