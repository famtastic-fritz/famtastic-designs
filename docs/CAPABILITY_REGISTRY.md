# FAMtastic Designs capability registry

This registry records what FAMtastic can responsibly demonstrate and sell. A
capability is not “proven” merely because code exists.

| Capability | Current evidence level | Reusable service value |
|---|---|---|
| Source-attributed creative prompt cookbook | Locally proven training and receipt contract | `marketing/creative/prompt-cookbook/` gives operators reusable image, edit, transparent-ingredient, image-to-video, and presenter templates with explicit fields, provider notes, source links, and required output receipts. It is a governed training system, not a claim that every connected provider has been output-proven. |
| Declarative multi-format campaign video system | Locally proven with rendered 9:16 proof and still QA in 9:16, 1:1, and 16:9 | A single source-backed scene configuration can assemble a branded campaign cut in all three formats without crop-only variants. The local `marketing/video/src/system/` kit enforces safe areas, one glow, format-aware typography, source-bound offers, aspect-specific plates, and an exact-but-readable CTA signature. First proof: `PlatformDependency-*`, rendered to `campaigns/own-website-vs-rented-platforms/videos/platform-dependency-9x16.mp4`. Local creative proof only; no upload, schedule, or delivery has been proven. |
| Drupal + React customer portals | Production smoke-tested & Design DNA v1 compliant | Branded account, service, support, learning, referral, retention, and token-scoped workspace control hubs governed by FAMTASTIC_CLIENT_PORTAL_DESIGN_DNA_V1 with automated structural and zero-leakage validation |
| Scripted single-post blog publishing (Blog Factory step 6) | Production-proven, real editorial content published | `scripts/publish-blog-draft.py` validates a `marketing/blog/drafts/<slug>/` folder (draft.md + brief.md + seo-check.json) against every required blog_post field, computes word count, converts the draft body to basic_html, and — only with `--confirm` — publishes via SSH + `vendor/bin/drush php:script backend/scripts/publish-single-blog-post.php` against production (the same SSH target and Drush mechanism `scripts/deploy-backend-godaddy.sh` already uses for the 64-article seed; no new credential). Idempotent by `field_content_key`. Proven end-to-end against production 2026-09-04 with a throwaway self-test node (created, updated in place, deleted, nid churn 152-155). Then used for real: 3 real posts published (nid 156-158) — `what-does-199-website-include`, `proof-first-website-see-before-you-pay`, and `why-running-business-on-gmail-and-linktree-costs-revenue` (the last written specifically because a live campaign post had been linking to it since 2026-09-03 with no article behind it). 2026-09-04 (later session): the 5 remaining drafts (`business-email-on-your-own-domain`, `how-local-customers-find-your-business-online`, `what-website-maintenance-actually-covers`, `do-you-guarantee-google-rankings`, `what-happens-when-first-year-hosting-ends`) were written/rewritten to full-length (376-449 words each), classified in `DRAFT_CLASSIFICATION`, and each passed `--dry-run` cleanly at the time. None have been published — that remains a `--confirm` decision for a human. **Correction, 2026-09-04 (later session 4):** the pipeline was publishing series orphans. `blog_post` is series-first (`field_blog_series` + `field_series_order`, both read by `BlogPostPage.jsx` for the prev/next series nav and the BreadcrumbList JSON-LD), and neither script set either field, so all three real posts (nid 156-158) shipped without them while all 80 seeded posts had them. Both scripts now require an explicit series and a non-colliding `series_order`, validated before any SSH contact and re-checked server-side; the series term is resolved by name and never created without an explicit `allow_create_series` opt-in. The three live posts were backfilled with the fixed pipeline (bodies confirmed byte-identical first, so only series metadata changed) and verified in a real browser on production. Consequently the 5 unpublished drafts **no longer pass `--dry-run`**: they fail loud on a missing series until a human assigns one, which is the intended behavior, not a regression. Also added `scripts/suggest-next-blog-topic.py` (read-only gap-finder: plan vs. drafts vs. live JSON:API) and `docs/playbook/RECIPES/BLOG_AUTOMATION_OPTIONS.md` (decision doc for making `scripts/qa-content-links.py` run automatically — no mechanism chosen yet). |
| Content link-QA audit | Locally proven, one full pass completed | Read-only crawl of every published `blog_post`'s body via JSON:API, extracting every internal/external link and checking each live with a real HTTP request (following redirects to final status). Run 2026-09-04 as a background agent against all 83 published posts (762 link occurrences, 97 unique targets): found zero instances of the `/web/` backend-prefix bug in production content (it existed only in unpublished draft source files, fixed before publish) and zero sibling-post 404s, but found one real dead external citation (an ICANN URL) reused across 8 posts in the fifty-five-cents-a-day series — fixed live via `drush eval` across all 8 nodes, and in the source seed config (`backend/config/famtastic-content-series.json`, `scripts/build-demand-library.py`) so a re-seed can't reintroduce it. Turned into a real, repeatable tool same-day: `scripts/qa-content-links.py` (read-only, no credential needed — public JSON:API + plain HTTP checks), re-run for real against production immediately after being built: 83 posts, 96 unique links, PASS, confirming the fixes above actually hold. `.agents/skills/famtastic-content-qa/SKILL.md` documents when/how to invoke it. Not yet scheduled/automatic — still a manually-invoked script, not a cron job or CI gate. |
| Adobe desktop creative production (Photoshop) | Locally proven, one real artifact produced end to end | Branded stills composed under agent control through the local Photoshop MCP bridge (`mcp__photoshop-bridge__*`, `tools/adobe-automation/photoshop-mcp`) against the paid Creative Cloud subscription — no Adobe cloud entitlement, no API credential, no per-image cost. Proven 2026-09-04 by composing and exporting a Design-DNA-v1-compliant 1080×1920 story frame (`#070907` ground, `#7cfc00` accent, type set in Photoshop, not burned by ffmpeg): `marketing/creative/adobe-proofs/cost-is-not-the-reason-story-1080x1920.png` (**copy superseded 2026-09-04 — it says the bundle is "maintained", which the catalog contradicts; kept only as the first end-to-end proof, never reuse its copy**), with the reusable template now living as parametrised code at `marketing/creative/templates/famtastic-social-frame.jsx`. **Boundary: locally proven only.** No Adobe-produced asset has yet been published to a customer or a channel, so this is not production-proven and not publish-proven. Preconditions and known bugs are real and documented in `docs/playbook/RECIPES/ADOBE_CREATIVE_PRODUCTION.md`: Photoshop must be *running* (the bridge drives the live app, it cannot launch it); `ps_create_text` accepts ASCII only (a cent sign either errors with `Required value is missing` or renders mojibake) and takes `fontFamily` (PostScript name), not `font`. Known gap: the brand faces Inter and Space Grotesk are not installed on this machine and Adobe Fonts has zero activations, so output substitutes HelveticaNeue-Condensed/AvenirNext. Premiere Pro's bridge exists but is **unproven** — `ping` timed out because the app was closed; 16 of the 18 installed Adobe apps have no bridge at all. |
| Drupal Commerce product lifecycle | Test-provider proven | Catalog-to-order-to-intake-to-entitlement delivery |
| Owner-managed microsite preorders with direct payment handoff | Locally proven; production activation pending owner configuration | Durable product/quantity/pickup requests, exact price snapshots, owner-generated Cash App link/QR, and phone-first manual payment/fulfillment status without FAMtastic processing or auto-verifying funds |
| Product onboarding contracts | Locally proven | Repeatable product, terms, intake, fulfillment, communication, reporting, and acceptance setup |
| Website discovery and package recommendation | Locally proven | Needs-led intake that distinguishes packaged and custom work |
| Owner-invited deep website discovery | Production deployed; targeted release and cross-account portal boundary verified | Private, answer-at-a-time discovery linked to a verified same-email workspace, with Booksy bridge, brand, content, local-growth, payment-display, asset-consent, and six-direction owner-review inputs. Release `1e0f82cb` accepts normal scheme-less public booking URLs and blocks draft proof dispatch; production explicitly refuses a proof link opened by a different signed-in account. A real provider proof, customer delivery, payment, booking, and launch remain separate gates. |
| Website-delivery specialist swarm | Locally proven | Versioned brief, package/add-on reasoning, three-direction proof, agent trace, independent QA, and Playwright evidence across guest and member lanes |
| Synthetic human-experience testing | Locally proven | Reusable control persona plus disclosed, opt-in creative lenses that cannot affect commercial, legal, safety, accessibility, or approval decisions |
| Account-scoped private pricing | Locally proven | Auditable relationship, partner, and promotional pricing without public coupons |
| Owner-gated three-direction website proofs | Production-deployed; customer delivery not yet proven | Safe/Wild/OMG generation, private staff review, authenticated selection, revision workflow, a same-email public-preview claim that cannot auto-create a generic request/job before verification, a verified-cold boundary requiring research, public-content redaction, exact-ID owner review, a private Build-DNA importer that rejects generic cold callbacks, and explicit transport gates before SMTP, plus a durable exact-ID pilot lock that blocks broad cron/lifecycle dispatch. The schema, route shells, lock, and exact cold-260 quarantine are production-deployed; a real provider build, staged anonymous room, same-email claim, and customer mail receipt remain unproven. |
| Anonymous view-only proof sharing | Production smoke-tested | Signed unlisted proof rooms load on apex and `www`; anonymous API access exposes only permitted directions while invalid signatures return no data |
| Revision loop with proof-version receipts | Locally proven (not yet deployed) | Customer revision request → allowance-gated $75 add-on purchase (stub/test gateway) → limit increment → owner alert + customer receipt; immutable per-project proof version history with prior versions preserved (`scripts/e2e-revision-loop.sh`, 15/15) |
| Off-session hosting renewal charging | Research complete; scaffold only; hard-disabled by code until Fritz gates open | R1 recommendation + never-charging renewals-cron scaffold in `docs/audits/R1-RENEWAL-CHARGING-RESEARCH.md`; no live charge capability claimed |
| Optional six-direction FAMtastic showcase | Locally proven | Three additional original high-intensity sites appended without bypassing private review or customer-send gates |
| Scoped service grants and owner comps | Locally proven | Hashed account/request codes with audited zero-dollar Commerce fulfillment |
| Private customer visual references | Locally proven | Consent-tracked, checksummed image and PDF inputs attached to one request |
| Lead response and notifications | Test-provider proven | Acknowledgment, owner alerts, deadlines, escalation, and worker protection |
| Lead attribution capture (UTM + click IDs) | Locally proven (not yet deployed) | utm_source/medium/campaign/content/term + gclid/fbclid snapshotted as one JSON field on the prospect at public and portal lead creation, social-record lead counters, and a content-grain leads→requests→paid-revenue view in Marketing Command Center |
| FAMtastic Concierge to Connections event bridge | Locally proven | Signed, idempotent email/iMessage lifecycle metadata projected onto the Drupal lead timeline without autonomous replies or commercial authority |
| Portal support cases | Test-provider proven | Customer timelines, priority targets, replies, and staff alerts |
| Mail reply ingestion | Locally proven | Thread-address matching, validation, attachment policy, and unmatched-reply handling |
| Google Analytics operations reporting | Production smoke-tested | Separate traffic/campaign reporting and customer analytics entitlement |
| SEO and dynamic sitemap delivery | Production smoke-tested | Drupal content discovery, route-specific shells, sitemap, robots, and canonical metadata. 2026-09-04: found and fixed a systemic canonical-tag defect — every node's canonical meta tag resolved to Drupal's internal `/web`-prefixed backend path (a dead link publicly) instead of the real frontend URL, because the shared `metatag.metatag_defaults.node` default's `[node:url]` token inherits Drupal's own request-root prefix. Fixed once at the shared source via `famtastic_pipeline_metatags_alter()`, not per-node; re-verified live across multiple posts post-fix. |
| Synthetic customer journey proof | Locally proven | Repeatable lifecycle QA with explicit provider and production boundaries |
| Isolated customer site deployment | Locally proven | Build, proof, approval, deployment, rollback, domain, and hosting lifecycle patterns |
| Preview-to-Site-Studio build packet bridge | Locally proven | Provider-neutral one/two-direction build packet, exact stage journal, signed success validation, project-scoped portal continuation, and transactional notification handoff |
| Six-direction proof retention library | Locally proven | Deduplicated design/structure candidates retained without reusing customer copy or media |
| Provider availability and build-class routing | Locally proven | Free, low, medium, premium, premium-brain/free-worker, and custom policies with fail-closed preflight and declared fallbacks |
| Lean research-first social-presence proof | Unlisted-hosting production smoke-tested | One coherent campaign identity, two purpose-built graphics, original character branding, editable social cards, responsive hub, exact model/cost ledger, and closed social-publication gates |
| Provider-neutral campaign image routing | One route proven; alternatives documented | Separates image capability, transport, and model so direct OpenAI or benchmarked alternate providers can replace OpenArt without silently lowering quality |
| Public FAMtastic Lab case-study publishing | Production smoke-tested | Rights-bounded public showcase, live-proof launch, promotional metadata, PII-free GA4 events, attributed intake CTA, anonymous desktop/phone verification, and durable evidence |
| Adjustable preview quality and telemetry contract | Locally proven | Provider-neutral build classes, FAMtastic intensity, typography/texture/depth gates, per-stage model/cost/duration records, one consolidated repair, and a two-review ceiling |
| Build DNA evidence and Site Studio continuity | Locally proven | Immutable build-level model, prompt, input/output, asset hash, timing, cost, fallback, QA, reviewer, filesystem, Drupal, and handoff lineage; verified-cold cohorts now require an exact checksummed Prospect/proof-campaign/public-campaign/job/callback runtime binding before finalization or callback serialization |
| Reusable page/component/part website recipes | Locally proven; Site Studio import unproven | Source-defined page recipes and stable section/component/field/slot identity are proven two ways: a controlled four-page single-media swap with nine frozen components, and four materially different research-backed recipes with eleven reusable components, sixteen cited decision records, four premium parent images, twelve reference-led companion images, and desktop/mobile browser QA. Arbitrary component swapping, multi-page conversion, production delivery, and Site Studio execution remain separate gates |
| Gemini Flash Lite reference-led image series | Provider route proven across two unrelated builds; build-level evidence locally proven | The FAMU-adjacent sequence and the unrelated 12-direction Booked & Branded series both completed at 1K with interaction IDs, usage, timings, exact prompt/reference/output hashes, and Build DNA. The latter also proves the quality-repair loop: generated text/UI artifacts were rejected, a photo-only prompt and targeted replacement produced the final set, and all 25 generations remain in the USD 0.8400 cumulative estimate. Owner review and real recipient delivery remain separate gates. |
| Gemini Developer Interactions API image chain | Historically proven; **route no longer resolves as of 2026-09-05** | A FAMU-adjacent reference-led Lite image and a stateful `previous_interaction_id` companion revision completed through the Developer API with real interaction IDs, usage, timings, image hashes, and no customer or production mutation. `POST /v1beta/interactions` now returns HTTP 404 for a model that exists, and `GET /v1beta/models` does not list `interactions` among any image model's `supportedGenerationMethods`. The working replacement is `POST /v1beta/models/<model>:generateContent` with `responseModalities: ['IMAGE']`; the stateful `previous_interaction_id` chain has **not** been re-proven on that call and must be treated as unproven until it is. |
| Gemini Flash Lite via `generateContent` | Provider route proven; build-level evidence locally proven | Five 9:16 cinematic plates for campaign drop-06 generated at `imageSize: '1K'` (768x1376; `'2K'` is rejected by this model) with per-image byte/prompt hashes, usage, timings and a USD 0.1680 receipt at `marketing/campaigns/cost-is-not-the-reason/images/broll/receipt.json`. Palette discipline was carried in the prompt, so plates arrive brand-graded rather than corrected afterwards. Owner review and publication remain separate gates. **2026-09-05 (separate session, Tier-2 proof for the platform-dependency blog series + Ghost Town Ep1):** `imageConfig.aspectRatio: "1:1"` proven for the first time in this repo (previously only `16:9`, `3:2`, `4:5`, `9:16` had a successful receipt) — output is 1024x1024 at `imageSize: '1K'`, confirming "1K" means roughly one megapixel, not a fixed edge length. One of 9 attempts was blocked (`finishReason: IMAGE_SAFETY`, not charged) on a prompt describing an isolated human silhouette at night; removing the person fixed it, and no other prompt describing a person in the same run was blocked. 8 plates, USD 0.2688 total, full per-image hashes/dimensions/durations, a 19-entry reusable prompt library, and the generator script are at `marketing/creative/plates/` (`prompt-library.json`, `generation-receipt.json`, `generate-plates.mjs`, `README.md`). Also found: `marketing/providers.json` has no `gemini_image`-style entry at all despite this being a working, keychain-credentialed, repeatedly-proven route — flagged in that README as a registry gap. Owner review and publication remain separate gates. |
| HeyGen brand kit and presenter lane | Provider route proven with render proof | Brand kit `8d249f1d06b4440ea665c539f206ecb7` imported from `https://famtasticdesigns.com` resolved to accent `#7CFC00` with Space Grotesk and Inter, matching the site's own `--v1-*` tokens; no logo was detected, consistent with BRAND.md's note that no final mark exists. One 39.63s presenter render of the private "FAMtastic Guide" photo avatar cost 12 premium credits and returned a word-timed sidecar SRT. Known limit: `background: {type:'color'}` is accepted and silently ignored for a photo avatar whose source image contains a full illustrated scene. |
| Three-tier branded social video assembly | Locally proven with FAMtastic render proof | Rendered 44.05s 1080x1920 h264+aac vertical drop (`Drop06GmailLinktree`, `marketing/video/src/drop06/`) combining a HeyGen presenter voice-over and on-camera bookends, five Gemini cinematic plates, and a Remotion brand layer carrying every word, colour and edge — ground `#070907`, single `#7cfc00` accent, one glow per screen, captions clear of the TikTok action rail. Total USD 0.168 plus 12 HeyGen credits. Beat and caption timings are derived from the provider SRT, not estimated. |
| Gemini Antigravity desktop bridge | Desktop sign-in and bridge discovery reported; provider execution unproven | Candidate local-attended reasoning, research, direction, copy, and prototype-construction route; it must pass a structured call, clean-session retry, failure fallback, and Build DNA receipt before autonomous use |
| HyperFrames designed-motion lane | Locally proven with FAMtastic render proof | Rendered 31.3s branded 9:16 vertical commercial (1080x1920 30fps) in 57.7s locally ($0 compute fee) with kinetic HUD badges, 55¢ / $199 calculation cards, single-glow #7CFC00 tokens, and synced AAC audio |
| MoneyPrinterTurbo narrative-video lane | **Historical output claim exists; current local runtime is unverified.** | Older benchmark files name four draft cuts, but the current checkout has no MoneyPrinterTurbo helper/install or run receipt. Treat it as an evidence-first local video candidate—not a currently proven route—until installation, credentials/media rights, output MP4, hashes, and QA are captured in one fresh job. Never auto-publish. |
| OpenArt & Kling 3 Omni 4K media lane | Provider route proven | Rendered 4K tactile craft stills (GPT-Image-2) and 5.04s full-motion action video (Kling 3 Omni) with synchronized audio |

The packet bridge's current evidence is contract-autonomous golden replay. A real
Site Studio success return, fresh provider-generated creative run, production
Drupal persistence, and live notification remain separate evidence upgrades.

The social-presence evidence covers an anonymously reachable unlisted static
experience plus a public FAMtastic-owned Lab case study. It does not prove
social account creation, social OAuth, scheduling, posting, engagement, or
official university approval. The OpenArt → GPT Image 2 route is proven for one
run; direct OpenAI and different-model fallbacks remain unproven until they pass
the same golden prompt and visual rubric.

The canonical reusable recipe is
`docs/architecture/LEAN_SOCIAL_PRESENCE_PRODUCTION_PROCESS_V1.md`. The Rattler
Lifers campaign remains its first golden example and does not become a general
template until a second unrelated brand reproduces the quality and timing.
| Campaign strategy and content systems | Production smoke-tested | Offer-led campaign architecture, landing pages, articles, FAQs, email, social adaptations, CTAs, UTMs, and measurement |
| Branded social creative production | Locally proven | Reusable image, motion-graphics, short-form video, caption, safe-area, and channel-variant production |
| Social publishing orchestration | **Schedule-proven 2026-09-03; still NOT publish-proven.** ROOT CAUSE of all prior failure: the Postiz `orchestrator` (Temporal publishing worker) was OOM-killed (`exit 137`) in a 3GiB colima VM from 2026-08-25 to 2026-09-03 — scheduled posts accumulated in QUEUE and none could ever publish. Fixed by resizing to 8GiB. All 16 provider records for `cost-is-not-the-reason` (4 drops × FB/IG/X/TikTok/YouTube) created and verified QUEUE per record (evidence `.artifacts/postiz-queue/cost-is-not-the-reason/1788437357/`). **No post has yet been confirmed live on any platform**; first genuine publish attempt 2026-09-03 09:00 ET. Five channels connection-proven 2026-08-25 (TikTok sandbox, daily re-auth, likely barred from public posting until app audit). 17-day campaign still unqueued beyond days 1–3, whose drafts carry stale 2026-08-23 dates, and whose DB approval gates remain closed (`import-social-records.php` treats the DB as authoritative, so opened manifest approvals do not propagate). Sending depends on the operator workstation; see `docs/marketing/CAMPAIGN_POSTING_ARCHITECTURE.md` | Approval, account-specific copy, media publishing, delivery verification, retry evidence, and campaign attribution |
| Campaign ad-CRUD mutation service | Locally proven, used for real | `scripts/queue-campaign-drops.py --add-drop/--edit-drop/--delete-drop <campaign>/<content_id> --set k=v [...] --confirm` (compound `campaign_id/content_id` keying, schema-validated before any Postiz call) plus a matching `PostizDropMutationService.php` for the admin UI. Used for real 2026-09-04 to add drop-05 to `cost-is-not-the-reason` from previously-unused creative. Correction: an earlier registry entry described a `--drop <N> --mutation k=v` / `mutations.jsonl` design that was never built — that entry was wrong, this replaces it. |
| YouTube publish path | **Retry repair locally deployed 2026-09-04; still NOT publish-proven — zero successful YouTube publishes to date.** The `ERR_SSL_SSL/TLS_ALERT_BAD_RECORD_MAC` path fault is intermittent packet corruption, not a network/MTU/Colima defect: the exact 20MB request replayed 5/5 from the container with full body transfer. It became permanent because gaxios excludes POST retry, `runInConcurrent()` converted every caught error to Temporal's non-retryable `BadBody`, and `YoutubeProvider` reused a consumed stream. Commit `ea5db55` changes transient failures to retryable and re-fetches the media stream per upload attempt; focused image tests proved `ECONNRESET` retries and HTTP 400 does not. The running `postiz` container uses `localhost/postiz-famtastic:youtube-retry-fix` and is healthy. **This proves repair mechanics and runtime readiness, not a successful YouTube publish.** The scheduled provider record remains the required delivery evidence. | Any claim that YouTube posting works; real retry exercise; delivery verification |
| TikTok Direct Post UX-compliance patch | Locally proven, patched image running | Self-hosted Postiz's stock TikTok composer hardcodes a `PUBLIC_TO_EVERYONE` privacy default and never displays creator identity — both explicit, documented TikTok Content Posting API audit rejection causes (confirmed via 3 open GitHub issues on gitroomhq/postiz-app: #1362, #1563, and the unmerged community fix #1761, closed only on a CLA/contributor-application technicality, not code quality). Cloned Postiz v2.22.1, applied PR #1761's 3-file diff, built `localhost/postiz-famtastic:tiktok-ux-fix` locally, swapped the running local container onto it (DB backed up first, postgres/redis untouched). Verified live in the running container: no hardcoded privacy default (starts on forced "Select"), creator nickname/account header now renders. Not yet used for a real TikTok publish — still blocked on the separate app-approval production-review audit (in progress) and the demo-video recording (needs a human for the one OAuth consent click). |
| Campaign publish-state scorecard | Locally proven; publish-state only, not conversion-proven | `scripts/score-campaign.py --campaign <slug>` reads a campaign's `posting-schedule.json` (program_id/series_id/drops, compound campaign_id+content_id key) and runs a read-only query against the local Postiz postgres for the real state of every recorded provider record, writing `marketing/campaigns/<slug>/scorecard.json` (schema `marketing/engine/schemas/campaign-scorecard.schema.json`). Run 2026-09-04 against the real `cost-is-not-the-reason` campaign: 16/18 requested channel records resolved (2 `linkedin` slots on drop-02 and drop-04 were never queued to a provider record and are reported by name, not silently dropped), 7 published, 8 error, 1 still queued — real states read live, not estimated. The schema deliberately carries no clicks/impressions/conversions/CTR/CPC fields; `clicks_conversions_available` is a hardcoded `false` with a required `gap_note` explaining Postiz's `Post` table has no click/impression column and GA4 reporting queries no utm_content dimension or conversion event. A separate `attribution_note` names — without scoring — the already-working AttributionService leads→requests→revenue join, which this script does not query. Never mutates a Postiz record. |
| Campaign operations admin UI | Code-merged 2026-09-03; functionality unproven in practice | Postiz mutation card and scorecard table in Operations Home Campaign Manager UI. Drop mutation UI chains to mutation service CLI for targeted overrides; scorecard table refresh calls read-only Postiz query and renders live states with error highlighting. Both read-only views with no production mutations. |
| AI-presenter video production | Provider generation in progress | Presenter-led explainers and advertisements governed by the same offer truth, QA, and publishing ledger |
| Marketing operations command center | Locally proven | Mobile campaign review, approvals, schedules, channel status, exceptions, attributed visits, leads, and sales |
| Project-shared marketing specialist core | Locally verified documentation and provenance | Pinned conversion, lifecycle, RevOps, campaign, discovery, and measurement methods shared by Codex, Claude, Shay, and future project agents; supporting methods only, not an autonomous or newly sellable capability |

| Business Email Setup (FAM-BUSINESS-EMAIL) | Provider-proven (commerce+entitlement locally; Stripe test wiring pending owner action) | Branded domain-email configuration, testing, and recorded handoff as a sellable $99 add-on |
| Website Maintenance Care Plan (FAM-MAINTENANCE) | Locally proven (recurring entitlement machinery; Stripe test wiring pending owner action) | Monthly care: update checks, backup verification, small content touches at $49.99/mo |
| Local SEO Setup (FAM-LOCAL-SEO) | Locally proven (catalog+entitlements; delivery semi-autonomous via SEO/GA4 tooling) | Structured local data, profile setup, and analytics verification at $299 one-time |

Update this table only when evidence changes. Link durable proof in the release
or acceptance report; do not upgrade a classification from confidence alone.
