(function (Drupal) {
  'use strict';

  // Mobile operations pages now carry their own, task-specific context.
  // Do not prepend a generic tutorial ahead of the owner's next action.
  Drupal.behaviors.famtasticOperationsMobile = {
    attach() {},
  };
})(Drupal);
