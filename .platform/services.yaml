# FAMtastic Designs — Platform.sh services (Phase 2 deployment scaffold).
#
# Consumed by the `backend` app via the `database` relationship
# (.platform.app.yaml). Credentials are injected into the app container as
# PLATFORM_RELATIONSHIPS and bridged into $databases by
# backend/web/sites/default/settings.php.

db:
  type: mariadb:10.11
  disk: 1024
