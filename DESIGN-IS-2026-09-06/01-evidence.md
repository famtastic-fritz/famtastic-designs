# Evidence

- Owner-supplied mobile screenshots show a large "Website Project Command
  Center" introduction before the active project, technical status labels, and
  a `Choose later` domain fallback.
- `PortalProjectsView.jsx` previously rendered project status, concepts,
  research, domain infrastructure, files, brief editor, Build DNA, sharing,
  and archive controls in one surface.
- A request with `revision_requested` was described as waiting while the old
  direction controls still rendered. The API correctly rejects those choices.
- Authoritative Drupal record for request 12 has a customer revision request:
  "Its very analytical, im scared it will scare my people away, maybe not user
  friendly." The three attached concepts are the original set.
- The generic revision acknowledgement used the `standard` template, which
  renders an operational footer and can show a raw portal URL. The proof-ready
  template has a Concierge frame but its old renderer also left the raw URL in
  the visible copy.
