#!/bin/sh
# FAMtastic Designs commit-msg guardrails.
# Soft check: AI attribution trailer reminder (does not block humans).
set -u

MSG_FILE="$1"
# Owner directive 2026-08-24: NO Co-Authored-By trailers, ever.
if grep -qi 'Co-Authored-By' "$MSG_FILE"; then
  echo "BLOCKED: Co-Authored-By trailers are forbidden in this repository." >&2
  exit 1
fi

exit 0
