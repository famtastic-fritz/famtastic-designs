# AlreadyBuilt owned-channel trial

## Contract and audience

This is the separately approved AlreadyBuilt experiment, not a new Drupal website package. Product truth is the independent private AlreadyBuilt repository, released trial commit `57eead0c4600407f2a17b36d07beee0c54cbc135`, `docs/LIVE-RUNBOOK.md`, and the live offer at https://alreadybuilt.famtasticdesigns.com/. The trial ends at **2026-10-02T23:15:26Z**; setup days count.

Audience: people who already operate a public website and need to understand detected technical issues. This campaign does not replace FAMtastic's broader educational mission or first-website offers. Category: Get Found. Topics: website strategy, website performance, analytics. Reader job: distinguish a detected finding from an assumed fix and a verified result.

The one-page free preview is optional and the informational Fix Map is **$39 once**. No subscription, automatic repairs, revenue/ranking promise, fabricated customer result or unobserved before/after example. Google PageSpeed Insights is a free alternative. Official reference: https://developers.google.com/speed/docs/insights/v5/about .

The current hosted sandbox acceptance includes payment, independent webhook verification, private report, mail, duplicate suppression and recovery. Live payment acceptance is still awaiting a real external buyer; neither the owner smoke checkout nor the owner email counts as revenue.

## Series and publication boundary

Series thesis: understand one real finding, decide whether a proposed repair is useful, then verify the outcome. Planned sequence: introduction; a documented owned-page example; how to read the paid map. Only the introductory Facebook drop is currently represented in the execution manifest. Later media requires its own source/evidence, factual QA, rights checks and Build DNA before scheduling. Nothing is queued merely because it appears in this README.

Fritz approved the trial's owned-channel campaign after end-to-end tests and specifically authorized FAMtastic Designs accounts/resources. This records that campaign-level authority, not per-post owner review. Agent QA must pass before the manifest approval flags and environment arming switch are enabled. No paid promotion, cold outreach, email list export or other account is included.

Use the existing `scripts/queue-campaign-drops.py` against **only** this campaign. Text/link is intentional, supported by its empty media list; it is not a silent fallback. The base URL includes a public `utm_id` because the existing runner appends remaining parameters with `&`. A dry-run plus parsed final URL check must precede provider contact.

Target: existing Facebook Page integration `cmsqql1yi0001ue7cchyfzhia`, **FAMTastic Designs**, not the founder's personal X account. On September 19 at 22:48 UTC, its database flag was enabled with no refresh required; publication must still be proved. Instagram's existing queue is not modified. TikTok is disabled; YouTube has no successful publication recorded. No connections are added.

## Verification and measurement

Record the exact Postiz ID, stored copy, date and state after draft creation. Arm only that ID via the existing runner. A `QUEUE` response is not a public post. Read back `PUBLISHED` plus its public release URL and inspect the platform page; if unavailable, report the limitation. Never retry a create on an uncertain result without inspecting the unique campaign/content marker first.

If the first publication remains queued or errors, stop expansion and inspect only that record. Do not restart all workers or replay other campaigns. The existing Facebook/Instagram backlog is 34 overdue records; eight YouTube records are also overdue. The currently running Postiz orchestrator reports online with zero restarts, so the historical OOM cause has not been re-established.

**Current block, 2026-09-19T23:07:22.102Z:** exact post `cmu8zjjhz0010rq76qt0wo4b2` remains QUEUE with no release URL or provider error. Its Temporal workflow has only two history events, the initial task is still Scheduled, and both the main workflow queue and Facebook activity queue report zero pollers. Process-online status is not worker readiness. Expansion is paused; owner approval was requested for a shared-scheduler repair that first holds back and preserves the 42 overdue records. No such hold or repair has occurred. This scheduled record remains eligible if the scheduler is restored; do not create a replacement. See `RECEIPT-20260919.md`.

CTA: Start with the free check. Stage: awareness. Campaign marker: `fix_map_trial_20260918`; unique content marker: `ab_intro_facebook_20260919`. UTM labels are link identifiers, not proof that analytics collection works. Compare aggregate host requests, durable scans, checkout intents and provider-backed external payments/deliveries. Exclude owner/testing activity; bot filtering cannot prove a human visitor. Post-level purchase attribution remains unproven. Do not claim a conversion rate from these data.

Useful answers: the report does not edit a website; the free tool can be used without the paid report; payment is one-time; the report is for one authorized public page. Related existing educational series: `small-business-website-strategy` and `website-analytics-decisions` in `backend/config/famtastic-content-series.json`. No blog, FAQ, schema or Drupal publication is created by this social introduction.

## Local checks

Run `python3 scripts/new-campaign.py --validate alreadybuilt-fix-map-trial`, the queue runner's `--dry-run`, and the demand checks before completion. Preserve the owner checkout's unrelated dirty work. This isolated branch is not an agency frontend/backend release.

September 19 pre-publication checks: scoped campaign validator and dry-run pass; 16 assertions cover channel/cutoff/copy/final UTM URL/HTTP 200. The live browser shows the matching free preview, $39 one-off report, no automatic repairs, and free PageSpeed alternative. Frontend build and campaign-readiness pass. Existing dependencies were reused through an ignored symlink, with no installation or subscription. The mandatory broader demand command was run but fails on the unchanged `own-your-online-presence` series (missing eight-post/pillar requirements); the owner checkout reproduces the same failure. No blog/Drupal content is changed, and no full demand-library pass is claimed.
